#include <bits/stdc++.h>

using namespace std;

int main(){
    //sort ,reverse 
    //int n;cin >> n;
    //int a[n];
    //for(int i = 0;i < n;i++)
    //    cin >> a[i];
    //sort(a+1,a+n-1);
    //sort(name+st,a+end);
    //reverse(a+1,a+n);
    //
    //int a,b;cin >> a >> b;
    //cout << a << " " << b << "\n";
    //swap(a,b);
    //cout << a << " " << b;
    //for(int i = 0;i < n;i++)
    //    cout << a[i] << ' ';    
}
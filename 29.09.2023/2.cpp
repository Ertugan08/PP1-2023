#include <bits/stdc++.h>

using namespace std;
int main() {
    //begin,end
    //s.begin 
    //s.begin(),s.end()
    string s;
    cin >> s;
    //sort(a+l,a+r)
    //sort(0,s.size())
    //sort(0,s.size());
    sort(s.begin(),s.end());
    cout << s;
    return 0; 
}